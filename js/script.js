const numbers = [1, 2, 3, 4, 5, 6];

for (let i = 0; i < numbers.length; i++) {
	console.log(numbers[i]);
}

function maths(x, y, z) {
	console.log(x + y * z);
}
maths(1, 2, 3);
if (numbers.length > 5) {
	console.log(true);
} else {
	console.log(false);
}
